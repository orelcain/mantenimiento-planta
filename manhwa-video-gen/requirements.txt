# Manhwa Video Generator — Python dependencies
# Install with: pip install -r requirements.txt

# Video assembly
moviepy>=1.0.3

# HTTP (ComfyUI API)
requests>=2.31.0

# TTS — primary (local, free, high quality)
kokoro-onnx>=0.3.0
soundfile>=0.12.1

# TTS — fallback (no model files needed)
pyttsx3>=2.90

# Image utilities
Pillow>=10.0.0
