{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",

    "devices": {
      "$deviceId": {
        ".read": "auth != null",
        ".write": "auth != null || newData.child('online').exists()"
      }
    },

    "sensors": {
      "$equipmentId": {
        ".read": "auth != null",
        ".write": "auth != null || (newData.child('temperatura').exists() && newData.child('humedad').exists())",
        "readings": {
          ".indexOn": ["timestamp"]
        }
      }
    }
  }
}
