{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",

    "devices": {
      "$deviceId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "sensors": {
      "$equipmentId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
