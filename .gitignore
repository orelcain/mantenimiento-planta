node_modules
dist
*.log
.turbo
*.tsbuildinfo

# Python venv
.venv

# Build outputs
apps/pwa/dist
apps/pwa/vite.config.js
apps/pwa/vite.config.d.ts

# Map images (uploaded by users, stored in Firebase)
*.png
apps/pwa/public/maps/*.png
# Excepción: imagen aérea de referencia del recinto (asset del proyecto)
!apps/pwa/public/maps/recinto-2023.png
# Excepción: base del módulo Planos de Aguas (asset del proyecto, requerido por la app)
!apps/pwa/public/planos-aguas-assets/plano-base.png
!apps/pwa/public/planos-aguas-assets/plano-base-dark.png

# Ruflo / claude-flow agent artifacts
.swarm/
setter_analysis.txt

# Local env files - IMPORTANTE: Nunca commitear credenciales
.env
.env.local
.env.*.local
apps/pwa/.env
apps/pwa/.env.local

# IDE
.idea
*.swp
*.swo

# VS Code workspace file (local)
*.code-workspace

# PlatformIO / ESP32 (no commitear builds ni credenciales)
**/.pio/
iot/**/config.h
iot/**/src/config.h
esp32-sensor/**/config.h
esp32-sensor/**/src/config.h


# Firebase Admin SDK (credenciales sensibles)
serviceAccountKey.json

# Caché local de Firebase Hosting (artefacto regenerable del CLI, genera churn)
.firebase/

# Archivos locales de trabajo (Excel Gantt)
gantt tareas.xlsx
~$gantt tareas.xlsx
GANTT Mantencion temporada baja.xlsm

# Cache local de scripts (Excel parseados, muy pesado)
scripts/.cache/

# Fixtures capturados de APIs externas (pueden contener data de producción)
scripts/fixtures/

# Cookies/credenciales de sesión para POCs locales — NUNCA commitear
.shoplogix-cookie.txt
.*-cookie.txt

# Scripts one-shot de diagnóstico (no comitear)
scripts/diag-fast.js
scripts/diag-p0-errors.js
scripts/diagnose-calibres.js
scripts/fase1_replace.py

# Proyecto independiente clonado dentro del repo
manhwa-video-gen/

# Carpetas staging para Project knowledge de claude.ai (regenerables, no versionar)
_claude_ai_bot_upload/
_claude_ai_upload/

# Traspaso de sesión (skill traspaso-limpio) — artefacto temporal, no versionar
/traspaso.md

# Fotos locales de los motores Sumitomo (seed) — se suben a Storage, no al repo
/scripts/sumitomo-photos/*
!/scripts/sumitomo-photos/README.md

# Backups / artefactos locales de migración y diagnóstico (no versionar)
*.bak-reconstruido
/backups/
/output/
__pycache__/
*.pyc
